# API Reference

This section collects the JavaScript entry points.

## `init()`

Initialize HET.

`init` accepts no parameters and does not return a value.

## `destroy()`

Destroy HET-managed runtime state.

`destroy` accepts no parameters and does not return a value.

